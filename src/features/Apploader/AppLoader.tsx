import { Oval } from "react-loader-spinner";

export default function AppLoader() {
  return (
    <div className="centered">
      <Oval color="#467fcf" height={300} width={300} />
    </div>
  );
}
